Step to initial the project:

1. Clone the repository - git clone https://github.com/n07Laksh/pg_redis_prisma_api-s.git

2. Install dependencies - npm install

3. Run - npm run prisma:migrate

4. Run - npm run prisma:generate

5. Run - npm run tsx:watch
